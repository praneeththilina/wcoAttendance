{
  "version": 2,
  "builds": [
    {
      "src": "backend/src/vercel.ts",
      "use": "@vercel/node"
    },
    {
      "src": "frontend/dist/**",
      "use": "static"
    }
  ],
  "installCommand": "pnpm --dir backend install --frozen-lockfile --lockfile-dir=backend && pnpm --dir frontend install --frozen-lockfile --lockfile-dir=frontend",
  "buildCommand": "pnpm --dir backend prisma generate && pnpm --dir backend exec tsc --project tsconfig.json && pnpm --dir frontend build",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/backend/dist/vercel.js"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/dist/index.html"
    }
  ]
}
