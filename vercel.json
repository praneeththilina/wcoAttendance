{
  "version": 2,
  "builds": [
    {
      "src": "backend/src/vercel.ts",
      "use": "@vercel/node"
    },
    {
      "src": "frontend/dist/**",
      "use": "static"
    }
  ],
  "installCommand": "cd backend && pnpm install --legacy-peer-deps --ignore-scripts && pnpm prisma generate && cd ../frontend && pnpm install",
  "buildCommand": "cd backend && npx tsc --skipLibCheck && cd ../frontend && pnpm build",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/backend/dist/vercel.js"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/dist/index.html"
    }
  ]
}