{
  "version": 2,
  "builds": [
    {
      "src": "dist/vercel.js",
      "use": "@vercel/node"
    }
  ],
  "installCommand": "cd backend && pnpm install --legacy-peer-deps",
  "buildCommand": "cd backend && pnpm prisma generate && pnpm exec tsc",
  "env": {
    "NODE_ENV": "production"
  }
}
